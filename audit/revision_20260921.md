# Scientific manuscript revision, 2026-09-21

Status: completed preprint revision. Initial source and PDF preserved under archive/pre_revision_20260921. Input SHA-256 values, environment, commands and failures are in the JSON twin.

Initial findings: split-count provenance conflict; closure criterion conflict; decoder notation typo; existing build does not check native process exit codes; release auditor trusts stale iteration records. Findings are under investigation. No training, remote mutation, or test event access.

## Evidence correction and reframing

Exact config-hash evidence confirms 26,624 pilot training events, not 551,234. The selected history has one declared 91-114 continuation. Canonical preparation records 76,158 validation / 76,300 test. The evaluator uses batch-maximum scaling, not the eventwise bound in the original paper. Revised framing: close mean counts coexist with fragmented support; no inference of full marginal or conditional fidelity. Native build exits are now checked before output replacement. Failed attempts and interrupted reviewer work are retained in the JSON record.

## Claim consistency correction

Batch-wide closure equations now match implementation; the old absolute-only countercheck is disclosed, without changing a guard. Raw-mode dust is a trivial counter, not a tiny-deposit test. Bootstrap endpoints are exploratory and reference halves are sample-size-mismatched. The headline is limited to close means with fragmented support. Literature metadata corrected. A simple algebraic counterexample separates the conceptual statement from the single-checkpoint observation.

## P1: wrong gap definition

The evaluator computes `mean_gaps = mean(span - active_count | nonempty)`: interior inactive layers, not maximal inactive runs. Values 6.05 and 2.10 are unchanged; all current labels and claims must use the actual statistic. QA22 passed but missed this semantic error and is superseded.

## Completed verification

Iteration 25 passes the complete unchanged scientific/build checks after correcting the findings. All 12 final rendered pages were inspected. Seven build/release guard regression tests pass. Twenty-six claim families and fourteen primary references are recorded in the claim register. Iterations 21, 23 and 24 remain recorded failures; iteration 22 is superseded following semantic and visual findings. No new training or event-level evaluation was performed.

The preprint supports a detector-specific validation study of one pilot checkpoint, framed as a HEP/computational-physics result. Physics fidelity, speed and causal architectural explanations require additional experiments. Authorship, acknowledgments and archival/data-release approval remain publication actions.
